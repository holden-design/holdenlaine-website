---
import SEO from '../../components/SEO.astro';
import Cursor from '../../components/Cursor.astro';
import HamburgerMenu from '../../components/HamburgerMenu.astro';
import TopNav from '../../components/TopNav.astro';

const PHOTO_CATEGORIES = {
  'fashion': {
    title: 'Fashion Product Photography',
    description: 'Fashion product photography and styled shoots',
    images: [
      "Fashion product photography/sonyaa.jpg",
      "Fashion product photography/sonya18.jpg",
      "Fashion product photography/eve1.jpg",
      "Fashion product photography/sonya17.jpg",
      "Fashion product photography/sonya16.jpg",
      "Fashion product photography/sonya15.jpg",
      "Fashion product photography/DSC00681.jpg",
      "Fashion product photography/sonyaa1.jpg"
    ]
  },
  'portrait': {
    title: 'Portrait Series',
    description: 'A series of portraits exploring light and emotion',
    images: ["portraits/_1533493.jpg", "Chew/_F5A0975.jpg", "portraits/1x1spotify.jpg", "portraits/IMG_4487.jpg", "portraits/portrait_new_3.jpg", "portraits/portrait_new_4.jpg", "portraits/portrait_new_5.jpg", "portraits/portrait_new_6.jpg", "portraits/portrait_new_7.jpg", "portraits/portrait_uuid_1.jpg", "portraits/portrait_uuid_2.jpg", "portraits/sonya1.jpg", "portraits/_F5A9764.jpg", "portraits/_F5A9777.jpg", "portraits/edit-10.jpg", "portraits/edit-11.jpg", "portraits/edit-3.jpg", "portraits/sonya6.jpg", "portraits/edit-4.jpg", "portraits/edit-5.jpg", "portraits/_MG_9779-2.jpg", "portraits/edit-6.jpg", "portraits/edit-7.jpg", "portraits/edit-8.jpg", "portraits/edit-9.jpg", "portraits/edit.jpg", "portraits/fucku.jpg", "portraits/fucku2.jpg", "portraits/holden2.jpg", "portraits/holden3.jpg", "portraits/1X1X2 copy.jpg", "portraits/july1.jpg", "portraits/july2.jpg", "portraits/karin1.jpg", "portraits/klara1.jpg", "portraits/klara3.jpg", "portraits/klara4.jpg", "portraits/klara5.jpg", "portraits/klara6.jpg", "portraits/mad1.jpg", "portraits/mad2.jpg", "portraits/marielle1.jpg", "portraits/more1.jpg", "portraits/more2.jpg", "portraits/sonya10.jpg", "portraits/sonya11.jpg", "portraits/sonya12.jpg", "portraits/sonya14.jpg", "portraits/sonya2.jpg", "portraits/sonya3.jpg", "portraits/sonya5.jpg", "portraits/sonya7.jpg", "portraits/sonya8.jpg", "portraits/victoria1.jpg", "portraits/_MG_0987-2.jpg", "portraits/_MG_0995.jpg", "portraits/edit-1.jpg", "portraits/edit-2.jpg"]
  },
  'editorial': {
    title: 'Editorial',
    description: 'Editorial photography and styled shoots',
    images: ["editorial/Screenshot 2025-11-25 at 21.20.01.png", "portraits/damn.jpg", "editorial/Screenshot 2025-11-25 at 21.20.15.png", "editorial/Screenshot 2025-11-25 at 21.20.22.png"]
  },
  'street': {
    title: 'Street / Events',
    description: 'Capturing candid moments from everyday life',
    images: ["Street/_F5A7588.jpg", "Street/_F5A7623-2 2.jpg", "Street/_F5A7631.jpg", "Street/_F5A7654.jpg", "Street/_F5A7661-2.jpg", "Street/_F5A7662.jpg", "Street/_F5A7710-2.jpg", "Street/_F5A7714.jpg", "Street/_F5A7725-2.jpg", "Street/_F5A7791-2.jpg", "Street/_F5A7791.jpg", "Street/_F5A7795.jpg", "Street/_F5A7814.jpg", "Street/_F5A7837.jpg", "Street/_F5A7846.jpg", "Street/_F5A7852.jpg", "Street/_F5A7855.jpg", "Street/_F5A7870.jpg", "Street/_F5A7873.jpg", "Street/_F5A7911.jpg", "Street/_F5A7940.jpg", "Street/_F5A9263.jpg", "Street/_F5A9268.jpg", "Street/_F5A9274.jpg", "Street/_F5A9279.jpg", "Street/_F5A9283-2.jpg", "Street/_F5A9285.jpg", "Street/_F5A9298.jpg", "Street/_F5A9310.jpg", "Street/_F5A9374.jpg", "Street/_F5A9486.jpg", "Street/_F5A9492.jpg", "Street/_F5A9493.jpg", "Street/_F5A9496-2.jpg", "Street/_F5A9497.jpg", "Street/_F5A9500.jpg", "Street/_F5A9503.jpg", "Street/adventure.jpg", "Street/adventure1.jpg", "Street/P1100300.jpg", "Street/P1100345.jpg", "Street/P1100418.jpg", "Street/P1100561.jpg", "Street/P1100570.jpg", "Street/P1100594.jpg", "Street/P1100864.jpg", "Street/images:background.jpg"]
  }
} as const;

export function getStaticPaths() {
  return [
    { params: { category: 'fashion' } },
    { params: { category: 'portrait' } },
    { params: { category: 'editorial' } },
    { params: { category: 'street' } }
  ];
}

const { category } = Astro.params;
const categoryData = PHOTO_CATEGORIES[category as keyof typeof PHOTO_CATEGORIES];
const categoryTitle = categoryData?.title || 'Photos';
const categoryDescription = categoryData?.description || 'Photography gallery';
const images = categoryData?.images || [];
const ogImage = images[0] ? `/images/${images[0]}` : '/images/og-image.jpg';

const categoryTranslations: {[key: string]: {titleEt: string, descriptionEt: string}} = {
  'fashion': { titleEt: 'Mood', descriptionEt: 'Moofotograafia ja stiliseeritud fotosessioond' },
  'portrait': { titleEt: 'Portreede Seeria', descriptionEt: 'Portreede seeria, mis uurib valgust ja emotsioone' },
  'editorial': { titleEt: 'Toimetuslik', descriptionEt: 'Toimetuslik fotograafia ja stiliseeritud pildistamised' },
  'street': { titleEt: 'T\u00e4navafotograafia / S\u00fcndmused', descriptionEt: 'Spontaansed hetked igap\u00e4evaelust' }
};

const translations = categoryTranslations[category as string] || { titleEt: categoryTitle, descriptionEt: categoryDescription };
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <SEO 
      title={categoryTitle}
      titleEt={translations.titleEt}
      description={categoryDescription}
      descriptionEt={translations.descriptionEt}
      image={ogImage}
      type="website"
    />
    <style is:global>
      body {
        margin: 0;
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        background: #fff;
        color: #222;
        -webkit-user-select: none;
        user-select: none;
      }
      img {
        -webkit-user-drag: none;
        user-select: none;
        -webkit-user-select: none;
      }
      #letterfall-canvas {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        pointer-events: none;
      }
      .container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 2rem;
        position: relative;
        z-index: 1;
      }
      .page-header {
        margin-bottom: 2rem;
      }
      .page-header h1 {
        font-size: 1.8rem;
        font-weight: 700;
        margin: 0;
        text-transform: uppercase;
        letter-spacing: 0.02em;
      }

      /* Masonry gallery */
      .masonry {
        columns: 4;
        column-gap: 0.75rem;
      }
      .masonry-item {
        break-inside: avoid;
        margin-bottom: 0.75rem;
        opacity: 0;
        transform: translateY(16px);
        transition: opacity 0.5s ease, transform 0.5s ease;
      }
      .masonry-item.visible {
        opacity: 1;
        transform: translateY(0);
      }
      .masonry-item img {
        width: 100%;
        height: auto;
        display: block;
        cursor: pointer;
        border-radius: 2px;
      }

      /* Lightbox */
      .lightbox {
        display: none;
        position: fixed;
        inset: 0;
        z-index: 9999;
        background: rgba(0,0,0,0.92);
        align-items: center;
        justify-content: center;
        cursor: pointer;
      }
      .lightbox.active { display: flex; }
      .lightbox img {
        max-width: 90vw;
        max-height: 90vh;
        object-fit: contain;
        cursor: default;
        opacity: 0;
        transition: opacity 0.2s ease;
      }
      .lightbox img.loaded { opacity: 1; }
      .lightbox-counter {
        position: fixed;
        bottom: 1.5rem;
        left: 50%;
        transform: translateX(-50%);
        color: rgba(255,255,255,0.7);
        font-size: 0.75rem;
        letter-spacing: 0.08em;
        pointer-events: none;
      }
      .lightbox-close {
        position: fixed;
        top: 1.5rem;
        right: 1.5rem;
        color: #fff;
        font-size: 1.5rem;
        cursor: pointer;
        opacity: 0.7;
        transition: opacity 0.2s;
        background: none;
        border: none;
        z-index: 10000;
        line-height: 1;
      }
      .lightbox-close:hover { opacity: 1; }
      .lightbox-nav {
        position: fixed;
        top: 50%;
        transform: translateY(-50%);
        color: #fff;
        font-size: 2rem;
        cursor: pointer;
        opacity: 0.7;
        transition: opacity 0.2s;
        background: none;
        border: none;
        padding: 1rem;
        z-index: 10000;
      }
      .lightbox-nav:hover { opacity: 1; }
      .lightbox-prev { left: 1rem; }
      .lightbox-next { right: 1rem; }

      @media (max-width: 1024px) {
        .masonry { columns: 3; column-gap: 0.6rem; }
      }
      @media (max-width: 768px) {
        .container { padding: 1rem; }
        .masonry { columns: 3; column-gap: 0.5rem; }
        .masonry-item { margin-bottom: 0.5rem; }
        .page-header h1 { font-size: 1.4rem; }
      }
      @media (max-width: 480px) {
        .masonry { columns: 2; column-gap: 0.4rem; }
        .masonry-item { margin-bottom: 0.4rem; }
        .container { padding: 0.5rem; }
      }
    </style>
  </head>
  <body>
    <canvas id="letterfall-canvas"></canvas>
    <TopNav />
    <Cursor />
    <HamburgerMenu />

    <div class="container">
      <div class="page-header">
        <h1>{categoryTitle}</h1>
      </div>

      <div class="masonry">
        {images.map((img: string, i: number) => (
          <div class="masonry-item">
            <img src={`/images/${img}`} alt={`${categoryTitle} ${i + 1}`} loading="lazy" data-index={i} />
          </div>
        ))}
      </div>
    </div>

    <!-- Lightbox -->
    <div class="lightbox" id="lightbox">
      <button class="lightbox-close" id="lightbox-close">&times;</button>
      <button class="lightbox-nav lightbox-prev" id="lightbox-prev">&#8249;</button>
      <button class="lightbox-nav lightbox-next" id="lightbox-next">&#8250;</button>
      <img id="lightbox-img" alt="" />
      <div class="lightbox-counter" id="lightbox-counter"></div>
    </div>

    <script is:inline src="/scripts/letterfall.js"></script>
    <script is:inline>
      (function() {
        // IntersectionObserver staggered fade-in
        var obs = new IntersectionObserver(function(entries) {
          entries.forEach(function(entry, i) {
            if (entry.isIntersecting) {
              setTimeout(function() { entry.target.classList.add('visible'); }, i * 60);
              obs.unobserve(entry.target);
            }
          });
        }, { threshold: 0.05 });
        document.querySelectorAll('.masonry-item').forEach(function(el) { obs.observe(el); });

        // Lightbox
        var lightbox = document.getElementById('lightbox');
        var lbImg = document.getElementById('lightbox-img');
        var lbCounter = document.getElementById('lightbox-counter');
        var allImgs = Array.from(document.querySelectorAll('.masonry-item img'));
        var currentIdx = 0;

        function showImage(idx) {
          if (idx < 0 || idx >= allImgs.length) return;
          currentIdx = idx;
          lbImg.classList.remove('loaded');
          lbImg.onload = function() { lbImg.classList.add('loaded'); };
          lbImg.src = allImgs[idx].src;
          lbCounter.textContent = (idx + 1) + ' / ' + allImgs.length;
          // Preload adjacent
          if (allImgs[idx + 1]) { var p = new Image(); p.src = allImgs[idx + 1].src; }
          if (allImgs[idx - 1]) { var p = new Image(); p.src = allImgs[idx - 1].src; }
        }

        allImgs.forEach(function(img, i) {
          img.addEventListener('click', function() {
            showImage(i);
            lightbox.classList.add('active');
          });
        });

        document.getElementById('lightbox-close').addEventListener('click', function(e) {
          e.stopPropagation();
          lightbox.classList.remove('active');
        });
        lightbox.addEventListener('click', function(e) {
          if (e.target === lightbox) lightbox.classList.remove('active');
        });
        document.getElementById('lightbox-prev').addEventListener('click', function(e) {
          e.stopPropagation();
          showImage(currentIdx - 1);
        });
        document.getElementById('lightbox-next').addEventListener('click', function(e) {
          e.stopPropagation();
          showImage(currentIdx + 1);
        });

        document.addEventListener('keydown', function(e) {
          if (!lightbox.classList.contains('active')) return;
          if (e.key === 'Escape') lightbox.classList.remove('active');
          if (e.key === 'ArrowLeft') showImage(currentIdx - 1);
          if (e.key === 'ArrowRight') showImage(currentIdx + 1);
        });

        // Touch swipe
        var touchStartX = 0;
        lightbox.addEventListener('touchstart', function(e) { touchStartX = e.changedTouches[0].screenX; }, { passive: true });
        lightbox.addEventListener('touchend', function(e) {
          var dx = e.changedTouches[0].screenX - touchStartX;
          if (Math.abs(dx) > 50) showImage(currentIdx + (dx < 0 ? 1 : -1));
        });

        // Prevent right-click on media
        document.addEventListener('contextmenu', function(e) {
          if (e.target.tagName === 'IMG') e.preventDefault();
        });
        document.addEventListener('dragstart', function(e) {
          if (e.target.tagName === 'IMG') e.preventDefault();
        });
      })();
    </script>
  </body>
</html>
